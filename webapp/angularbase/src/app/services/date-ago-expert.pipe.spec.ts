import { DateAgoExpertPipe } from './date-ago-expert.pipe';

describe('DateAgoExpertPipe', () => {
  it('create an instance', () => {
    const pipe = new DateAgoExpertPipe();
    expect(pipe).toBeTruthy();
  });
});
