import { ReverseInnovatorPipe } from './reverse-innovator.pipe';

describe('ReverseInnovatorPipe', () => {
  it('create an instance', () => {
    const pipe = new ReverseInnovatorPipe();
    expect(pipe).toBeTruthy();
  });
});
